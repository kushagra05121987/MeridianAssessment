# UI Challenge — Dataset Visualization UI

Challenge: build a React + TypeScript page that lets an on-call engineer investigate
a usage-events dataset — filterable/sortable table, aggregate visualization, detail view,
URL-persisted state.

> **Status:** Not attempted within the assessment window.

The challenge description is available at:
`GET /api/v1/challenges/ui` (Authorization required)
