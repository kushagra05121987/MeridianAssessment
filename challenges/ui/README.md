# UI Challenge

Build a React + TypeScript page for investigating a usage-events dataset —
filterable/sortable table, aggregate charts, detail view, URL-persisted state.

**Status: Not attempted within the assessment window.**

The full brief is at `GET /api/v1/challenges/ui` (auth required).
