# Design Challenge — Service Design with Infrastructure Constraints

Challenge: build a rate-limited API-key issuance service using Redis (state),
Postgres (durability), and AES-GCM + HKDF (record encryption), shipped via docker-compose.

> **Status:** Not attempted within the assessment window.

The challenge description is available at:
`GET /api/v1/challenges/design` (Authorization required)
