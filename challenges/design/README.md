# Design Challenge

Build a rate-limited API-key issuance service using Redis (state), Postgres (durability),
and AES-GCM + HKDF for record encryption, shipped via docker-compose.

**Status: Not attempted within the assessment window.**

The full brief is at `GET /api/v1/challenges/design` (auth required).
